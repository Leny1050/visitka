// Добавьте необходимые функции JavaScript здесь, если они будут добавлены в будущем

// Пример функции для кнопки
document.querySelector('button').addEventListener('click', function() {
    window.scrollTo({
        top: document.body.scrollHeight,
        behavior: 'smooth'
    });
});

